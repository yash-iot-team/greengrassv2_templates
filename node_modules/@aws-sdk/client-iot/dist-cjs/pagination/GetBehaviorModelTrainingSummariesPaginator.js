"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.paginateGetBehaviorModelTrainingSummaries = void 0;
const GetBehaviorModelTrainingSummariesCommand_1 = require("../commands/GetBehaviorModelTrainingSummariesCommand");
const IoT_1 = require("../IoT");
const IoTClient_1 = require("../IoTClient");
const makePagedClientRequest = async (client, input, ...args) => {
    return await client.send(new GetBehaviorModelTrainingSummariesCommand_1.GetBehaviorModelTrainingSummariesCommand(input), ...args);
};
const makePagedRequest = async (client, input, ...args) => {
    return await client.getBehaviorModelTrainingSummaries(input, ...args);
};
async function* paginateGetBehaviorModelTrainingSummaries(config, input, ...additionalArguments) {
    let token = config.startingToken || undefined;
    let hasNext = true;
    let page;
    while (hasNext) {
        input.nextToken = token;
        input["maxResults"] = config.pageSize;
        if (config.client instanceof IoT_1.IoT) {
            page = await makePagedRequest(config.client, input, ...additionalArguments);
        }
        else if (config.client instanceof IoTClient_1.IoTClient) {
            page = await makePagedClientRequest(config.client, input, ...additionalArguments);
        }
        else {
            throw new Error("Invalid client, expected IoT | IoTClient");
        }
        yield page;
        token = page.nextToken;
        hasNext = !!token;
    }
    return undefined;
}
exports.paginateGetBehaviorModelTrainingSummaries = paginateGetBehaviorModelTrainingSummaries;
